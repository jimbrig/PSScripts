name: Publish
on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  publish:
    name: Publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Publish
        id: publish
        shell: pwsh
        run: |
          $apiKey = '${{ secrets.PS_GALLERY_API_KEY }}' | ConvertTo-SecureString -AsPlainText -Force
          $cred = [pscredential]::new('apikey', $apiKey)
          $env:NUGET_API_TOKEN = ${{ secrets.PS_GALLERY_API_KEY }}
          ./Build.ps1
